import java.util.List;

public class main {
    
	List<Integer> ints = new List<Integer>();


    ints.add(num1);
    ints.add(num2); 
    ints.add(num3);

    Integer ref = 0;
    for(Integer i = 0; i < 3; i++){
        if(ints[i] > ref){
            ref = ints[i]
        }
    }

    return ref;
    }

